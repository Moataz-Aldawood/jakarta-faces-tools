{
  "name": "jakarta-ee-tools",
  "author": "Moataz Aldawood and his friend Google Gemini ;)",
  "publisher": "moataz-aldawood",
  "displayName": "Jakarta Faces Tools",
  "description": "Advanced Jakarta Faces (JSF) tools: immediate quote auto-complete & smart EL snippets, Lombok annotation support (@Data, @Getter), incremental bean caching, EL semantic validation, component linking (for <-> id), deep nested Java EL resolution, custom composite component intelligence, and hover documentation. GitHub: https://github.com/Moataz-Aldawood/jakarta-faces-tools",
  "version": "3.0.0",
  "icon": "logo.png",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Moataz-Aldawood/jakarta-faces-tools"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "Jakarta EE",
    "JSF",
    "Java",
    "Java EE",
    "Coding Utilities",
    "PrimeFaces",
    "OmniFaces",
    "BootsFaces",
    "Unknown Tag Detection",
    "Unknown Attribute Detection"
  ],
  "activationEvents": [
    "onLanguage:jsf",
    "onLanguage:xhtml",
    "onLanguage:html",
    "onLanguage:xml",
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": [
      {
        "title": "Jakarta Faces Tools: EL Autocomplete",
        "properties": {
          "jakartaFacesTools.enableELAutocomplete": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "description": "Enable intelligent auto-complete for Java Managed Beans and properties inside Expression Language (EL) #{...} expressions."
          },
          "jakartaFacesTools.enableELDiagnostics": {
            "type": "boolean",
            "default": true,
            "order": 2,
            "description": "Enable real-time semantic diagnostics (warnings) for unknown Java Managed Beans and mistyped property names inside #{...} EL expressions."
          },
          "jakartaFacesTools.enableScopeWarnings": {
            "type": "boolean",
            "default": false,
            "order": 3,
            "description": "[Experimental Feature] Enable best-practice semantic warnings when a @RequestScoped Managed Bean is bound to a stateful UI component (e.g., <p:dataTable>, <h:dataTable>, <ui:repeat>), preventing pagination and sorting state loss across postbacks."
          },
          "jakartaFacesTools.enableIncrementalCache": {
            "type": "boolean",
            "default": true,
            "order": 4,
            "description": "Enable real-time Java file watching (**/*.java) and incremental cache updates when Managed Bean files are saved. Disable this in very large repositories or network drives if file watching causes performance issues."
          },
          "jakartaFacesTools.showRebuildCacheButton": {
            "type": "boolean",
            "default": true,
            "order": 5,
            "description": "Show the 'Rebuild JSF Cache' button in the VS Code Status Bar. Expression Language (EL) Autocomplete caches your Java Managed Beans in memory for fast performance; use this button to force a fresh scan of your project if beans change outside of normal edits."
          },
          "jakartaFacesTools.rebuildCacheButtonPosition": {
            "type": "string",
            "enum": [
              "Right",
              "Left"
            ],
            "default": "Right",
            "order": 6,
            "description": "Choose the position ('Right' or 'Left') of the 'Rebuild JSF Cache' button in the VS Code Status Bar when Expression Language (EL) Autocomplete is enabled."
          }
        }
      },
      {
        "title": "Jakarta Faces Tools: EL Highlighting",
        "properties": {
          "jakartaFacesTools.ELHighlighting.enable": {
            "type": "boolean",
            "default": true,
            "order": 10,
            "description": "Enable highlighting for Expression Language (EL) #{...} expressions."
          },
          "jakartaFacesTools.ELHighlighting.backgroundColor": {
            "type": "string",
            "default": "rgba(100, 150, 255, 0.15)",
            "order": 11,
            "description": "Background color for Expression Language (EL) #{...} expressions. Supports CSS colors (e.g. rgba, hex)."
          },
          "jakartaFacesTools.ELHighlighting.color": {
            "type": "string",
            "default": "",
            "order": 12,
            "description": "Text color for Expression Language (EL) #{...} expressions (leave empty to use theme default)."
          },
          "jakartaFacesTools.ELHighlighting.border": {
            "type": "string",
            "default": "1px solid rgba(100, 150, 255, 0.3)",
            "order": 13,
            "description": "Border for Expression Language (EL) #{...} expressions."
          },
          "jakartaFacesTools.ELHighlighting.borderRadius": {
            "type": "string",
            "default": "3px",
            "order": 14,
            "description": "Border radius for Expression Language (EL) #{...} expressions."
          },
          "jakartaFacesTools.showInlineBeanScopes": {
            "type": "boolean",
            "default": true,
            "order": 15,
            "description": "Enable inline editor badges (Inlay Hints) around Expression Language (#{...} / ${...}) blocks showing the Java Managed Bean's CDI / Faces scope (e.g. '@ViewScoped : ' or ' : @ViewScoped')."
          },
          "jakartaFacesTools.inlineBeanScopesPosition": {
            "type": "string",
            "enum": [
              "Pre-EL",
              "Post-EL"
            ],
            "default": "Pre-EL",
            "order": 16,
            "description": "Choose where inline editor scope badges appear relative to Expression Language blocks: 'Pre-EL' displays before the expression ('@ViewScoped : #{bean.property}') and 'Post-EL' displays after the expression ('#{bean.property} : @ViewScoped')."
          }
        }
      },
      {
        "title": "Jakarta Faces Tools: Documentation & Hover Cards",
        "properties": {
          "jakartaFacesTools.enableHoverCards": {
            "type": "boolean",
            "default": true,
            "order": 20,
            "description": "Enable rich documentation hover cards when hovering over JSF tags, attributes, namespaces, and component IDs."
          }
        }
      }
    ],
    "commands": [
      {
        "command": "jakartaFacesTools.rebuildJsfCache",
        "title": "Rebuild JSF Cache",
        "category": "Jakarta Faces Tools"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "jakartaFacesTools.rebuildJsfCache"
        }
      ]
    }
  },
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test:el": "node testElDiagnostics.js",
    "test:cache": "node testIncrementalCache.js",
    "test:lombok": "node testLombokSupport.js",
    "test:autocomplete": "node testImmediateAutocomplete.js",
    "test:scope": "node testBeanScopeAwareness.js",
    "test": "npm run compile && node testElDiagnostics.js && node testIncrementalCache.js && node testLombokSupport.js && node testImmediateAutocomplete.js && node testBeanScopeAwareness.js"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.1.3"
  }
}
